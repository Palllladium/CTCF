name: ctcf
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip>=24
  - setuptools
  - wheel
  - ninja
  - numpy
  - scipy
  - scikit-image
  - matplotlib
  - imageio
  - tifffile
  - ml-collections
  - pandas
  - pip:
      - torch
      - torchvision
      - torchaudio
      - natsort
      - tensorboard
      - tqdm
      - einops
      - timm
      - thop
      - pystrum
      - safetensors
      - huggingface-hub
      - markdown
      - surface-distance
      - werkzeug
